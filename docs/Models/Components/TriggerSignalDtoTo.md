# TriggerSignalDtoTo

Recipient(s). Accepts a subscriberId string, subscriber object, topic object, or an array of those. When omitted, Novu falls back to the conversation subscriber.


## Supported Types

### Str

```csharp
TriggerSignalDtoTo.CreateStr(/* values here */);
```

### MapOfAny

```csharp
TriggerSignalDtoTo.CreateMapOfAny(/* values here */);
```

### ArrayOfTo3

```csharp
TriggerSignalDtoTo.CreateArrayOfTo3(/* values here */);
```
