//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Novu.Models.Components
{
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using Novu.Models.Components;
    using Novu.Utils;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class SubscriptionsType
    {
        private SubscriptionsType(string value) { Value = value; }

        public string Value { get; private set; }

        public static SubscriptionsType Str { get { return new SubscriptionsType("str"); } }

        public static SubscriptionsType TopicSubscriberIdentifierDto { get { return new SubscriptionsType("TopicSubscriberIdentifierDto"); } }

        public override string ToString() { return Value; }
        public static implicit operator String(SubscriptionsType v) { return v.Value; }
        public static SubscriptionsType FromString(string v) {
            switch(v) {
                case "str": return Str;
                case "TopicSubscriberIdentifierDto": return TopicSubscriberIdentifierDto;
                default: throw new ArgumentException("Invalid value for SubscriptionsType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((SubscriptionsType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }

    [JsonConverter(typeof(Subscriptions.SubscriptionsConverter))]
    public class Subscriptions
    {
        public Subscriptions(SubscriptionsType type)
        {
            Type = type;
        }

        [SpeakeasyMetadata("form:explode=true")]
        public string? Str { get; set; }

        [SpeakeasyMetadata("form:explode=true")]
        public TopicSubscriberIdentifierDto? TopicSubscriberIdentifierDto { get; set; }

        public SubscriptionsType Type { get; set; }
        public static Subscriptions CreateStr(string str)
        {
            SubscriptionsType typ = SubscriptionsType.Str;

            Subscriptions res = new Subscriptions(typ);
            res.Str = str;
            return res;
        }
        public static Subscriptions CreateTopicSubscriberIdentifierDto(TopicSubscriberIdentifierDto topicSubscriberIdentifierDto)
        {
            SubscriptionsType typ = SubscriptionsType.TopicSubscriberIdentifierDto;

            Subscriptions res = new Subscriptions(typ);
            res.TopicSubscriberIdentifierDto = topicSubscriberIdentifierDto;
            return res;
        }

        public class SubscriptionsConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(Subscriptions);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    throw new InvalidOperationException("Received unexpected null JSON value");
                }

                var json = JRaw.Create(reader).ToString();
                var fallbackCandidates = new List<(System.Type, object, string)>();

                try
                {
                    return new Subscriptions(SubscriptionsType.TopicSubscriberIdentifierDto)
                    {
                        TopicSubscriberIdentifierDto = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<TopicSubscriberIdentifierDto>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(TopicSubscriberIdentifierDto), new Subscriptions(SubscriptionsType.TopicSubscriberIdentifierDto), "TopicSubscriberIdentifierDto"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                if (json[0] == '"' && json[^1] == '"'){
                    return new Subscriptions(SubscriptionsType.Str)
                    {
                        Str = json[1..^1]
                    };
                }

                if (fallbackCandidates.Count > 0)
                {
                    fallbackCandidates.Sort((a, b) => ResponseBodyDeserializer.CompareFallbackCandidates(a.Item1, b.Item1, json));
                    foreach(var (deserializationType, returnObject, propertyName) in fallbackCandidates)
                    {
                        try
                        {
                            return ResponseBodyDeserializer.DeserializeUndiscriminatedUnionFallback(deserializationType, returnObject, propertyName, json);
                        }
                        catch (ResponseBodyDeserializer.DeserializationException)
                        {
                            // try next fallback option
                        }
                        catch (Exception)
                        {
                            throw;
                        }
                    }
                }

                throw new InvalidOperationException("Could not deserialize into any supported types.");
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    throw new InvalidOperationException("Unexpected null JSON value.");
                }

                Subscriptions res = (Subscriptions)value;

                if (res.Str != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Str));
                    return;
                }

                if (res.TopicSubscriberIdentifierDto != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.TopicSubscriberIdentifierDto));
                    return;
                }
            }

        }

    }
}