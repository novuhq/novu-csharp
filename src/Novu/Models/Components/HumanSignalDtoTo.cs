//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Novu.Models.Components
{
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using Novu.Utils;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class HumanSignalDtoToType
    {
        private HumanSignalDtoToType(string value) { Value = value; }

        public string Value { get; private set; }

        public static HumanSignalDtoToType Str { get { return new HumanSignalDtoToType("str"); } }

        public static HumanSignalDtoToType ArrayOfStr { get { return new HumanSignalDtoToType("arrayOfStr"); } }

        public override string ToString() { return Value; }
        public static implicit operator String(HumanSignalDtoToType v) { return v.Value; }
        public static HumanSignalDtoToType FromString(string v) {
            switch(v) {
                case "str": return Str;
                case "arrayOfStr": return ArrayOfStr;
                default: throw new ArgumentException("Invalid value for HumanSignalDtoToType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((HumanSignalDtoToType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }

    /// <summary>
    /// Novu subscriberId(s) allowed to settle this interaction (max 50). First valid answer wins. When omitted, the conversation subscriber is used. Subscriber ids only — not workflow topic recipients.
    /// </summary>
    [JsonConverter(typeof(HumanSignalDtoTo.HumanSignalDtoToConverter))]
    public class HumanSignalDtoTo
    {
        public HumanSignalDtoTo(HumanSignalDtoToType type)
        {
            Type = type;
        }

        [SpeakeasyMetadata("form:explode=true")]
        public string? Str { get; set; }

        [SpeakeasyMetadata("form:explode=true")]
        public List<string>? ArrayOfStr { get; set; }

        public HumanSignalDtoToType Type { get; set; }
        public static HumanSignalDtoTo CreateStr(string str)
        {
            HumanSignalDtoToType typ = HumanSignalDtoToType.Str;

            HumanSignalDtoTo res = new HumanSignalDtoTo(typ);
            res.Str = str;
            return res;
        }
        public static HumanSignalDtoTo CreateArrayOfStr(List<string> arrayOfStr)
        {
            HumanSignalDtoToType typ = HumanSignalDtoToType.ArrayOfStr;

            HumanSignalDtoTo res = new HumanSignalDtoTo(typ);
            res.ArrayOfStr = arrayOfStr;
            return res;
        }

        public class HumanSignalDtoToConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(HumanSignalDtoTo);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    throw new InvalidOperationException("Received unexpected null JSON value");
                }

                var json = JRaw.Create(reader).ToString();
                var fallbackCandidates = new List<(System.Type, object, string)>();

                if (json[0] == '"' && json[^1] == '"'){
                    return new HumanSignalDtoTo(HumanSignalDtoToType.Str)
                    {
                        Str = json[1..^1]
                    };
                }

                try
                {
                    return new HumanSignalDtoTo(HumanSignalDtoToType.ArrayOfStr)
                    {
                        ArrayOfStr = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<List<string>>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(List<string>), new HumanSignalDtoTo(HumanSignalDtoToType.ArrayOfStr), "ArrayOfStr"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                if (fallbackCandidates.Count > 0)
                {
                    fallbackCandidates.Sort((a, b) => ResponseBodyDeserializer.CompareFallbackCandidates(a.Item1, b.Item1, json));
                    foreach(var (deserializationType, returnObject, propertyName) in fallbackCandidates)
                    {
                        try
                        {
                            return ResponseBodyDeserializer.DeserializeUndiscriminatedUnionFallback(deserializationType, returnObject, propertyName, json);
                        }
                        catch (ResponseBodyDeserializer.DeserializationException)
                        {
                            // try next fallback option
                        }
                        catch (Exception)
                        {
                            throw;
                        }
                    }
                }

                throw new InvalidOperationException("Could not deserialize into any supported types.");
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    throw new InvalidOperationException("Unexpected null JSON value.");
                }

                HumanSignalDtoTo res = (HumanSignalDtoTo)value;

                if (res.Str != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Str));
                    return;
                }

                if (res.ArrayOfStr != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.ArrayOfStr));
                    return;
                }

                throw new InvalidOperationException(
                    "Could not serialize union to JSON: no variant value was set. " +
                    "Construct this union using one of the Create* factory methods.");
            }

        }

    }
}