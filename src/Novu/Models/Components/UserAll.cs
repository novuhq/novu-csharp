//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Novu.Models.Components
{
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using Novu.Models.Components;
    using Novu.Utils;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class UserAllType
    {
        private UserAllType(string value) { Value = value; }

        public string Value { get; private set; }

        public static UserAllType WorkflowPreferenceDto { get { return new UserAllType("WorkflowPreferenceDto"); } }

        public override string ToString() { return Value; }
        public static implicit operator String(UserAllType v) { return v.Value; }
        public static UserAllType FromString(string v) {
            switch(v) {
                case "WorkflowPreferenceDto": return WorkflowPreferenceDto;
                default: throw new ArgumentException("Invalid value for UserAllType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((UserAllType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }

    /// <summary>
    /// A preference for the workflow. The values specified here will be used if no preference is specified for a channel.
    /// </summary>
    [JsonConverter(typeof(UserAll.UserAllConverter))]
    public class UserAll
    {
        public UserAll(UserAllType type)
        {
            Type = type;
        }

        [SpeakeasyMetadata("form:explode=true")]
        public WorkflowPreferenceDto? WorkflowPreferenceDto { get; set; }

        public UserAllType Type { get; set; }
        public static UserAll CreateWorkflowPreferenceDto(WorkflowPreferenceDto workflowPreferenceDto)
        {
            UserAllType typ = UserAllType.WorkflowPreferenceDto;

            UserAll res = new UserAll(typ);
            res.WorkflowPreferenceDto = workflowPreferenceDto;
            return res;
        }

        public class UserAllConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(UserAll);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    throw new InvalidOperationException("Received unexpected null JSON value");
                }

                var json = JRaw.Create(reader).ToString();
                var fallbackCandidates = new List<(System.Type, object, string)>();

                try
                {
                    return new UserAll(UserAllType.WorkflowPreferenceDto)
                    {
                        WorkflowPreferenceDto = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<WorkflowPreferenceDto>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(WorkflowPreferenceDto), new UserAll(UserAllType.WorkflowPreferenceDto), "WorkflowPreferenceDto"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                if (fallbackCandidates.Count > 0)
                {
                    fallbackCandidates.Sort((a, b) => ResponseBodyDeserializer.CompareFallbackCandidates(a.Item1, b.Item1, json));
                    foreach(var (deserializationType, returnObject, propertyName) in fallbackCandidates)
                    {
                        try
                        {
                            return ResponseBodyDeserializer.DeserializeUndiscriminatedUnionFallback(deserializationType, returnObject, propertyName, json);
                        }
                        catch (ResponseBodyDeserializer.DeserializationException)
                        {
                            // try next fallback option
                        }
                        catch (Exception)
                        {
                            throw;
                        }
                    }
                }

                throw new InvalidOperationException("Could not deserialize into any supported types.");
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    throw new InvalidOperationException("Unexpected null JSON value.");
                }

                UserAll res = (UserAll)value;

                if (res.WorkflowPreferenceDto != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.WorkflowPreferenceDto));
                    return;
                }
            }

        }

    }
}